spring:
  application:
    name: news
  profiles:
    active: dev
  elasticsearch:
    jest:
      uris: http://localhost:32769
  cloud:
    config:
      name: springCloudConfigService
      label: master
      profile: dev
      uri: http://localhost:7002

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.allenliu.news.mapper.*.*